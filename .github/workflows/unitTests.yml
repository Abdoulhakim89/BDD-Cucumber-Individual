name: run unit tests
on: 
  push:
    branches: 
     - "feature/*"
  
jobs:
  run-unit-tests:
   runs-on: ubuntu-latest

   steps:
    - name: Checkout
      uses: actions/checkout@v6.0.2

    - name: run unit tests
      run: mvn clean test
  

  
 
