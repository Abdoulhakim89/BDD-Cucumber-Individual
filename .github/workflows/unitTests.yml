name: run unit tests
on: push
  
jobs:
  run-unit-tests:
   runs-on: ubuntu-latest

   steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: run unit tests
      run: mvn clean test
  

  
 
